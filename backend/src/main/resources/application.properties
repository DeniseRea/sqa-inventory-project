# =============================================================================
# Cafeteria Don Gato - Inventory Management System
# =============================================================================

spring.application.name=dongato-inventory

# --- Database (auto-configured by spring-boot-docker-compose) ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# --- Server ---
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# --- Security Headers ---
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true

# --- Security users ---
app.security.admin.password=${ADMIN_PASSWORD:dongato2026}
app.security.user.password=${USER_PASSWORD:cafe123}

# --- Logging ---
logging.level.root=INFO
logging.level.com.dongato.inventory=DEBUG
logging.level.org.springframework.security=INFO

# Security Test: Hardcoded AWS Key to trigger Discord Webhook Alert
test.security.aws.key=AKIAIMNO7EXAMPLEDATA
test.security.aws.secret=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY


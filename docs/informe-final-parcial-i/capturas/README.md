# Capturas del informe

Estas capturas se usan como evidencia visual en `main.tex`.

- `captura_pipeline_github_actions.png`: historial de GitHub Actions con workflow DevSecOps y corridas exitosas.
- `captura_sonarqube_quality_gate.png`: estado final en SonarCloud con Quality Gate aprobado, 0 open issues, 0.0% duplicacion y 83.5% de cobertura overall code.
- `captura_sonarqube_before.jpeg`: estado anterior de SonarCloud con 23 issues abiertos.
- `captura_sonarqube_issues_before.jpeg`: detalle anterior de issues de mantenibilidad, 19 medium y 4 low.
- `portada-gato-don-gato.png`: imagen oficial del gato tomada desde `frontend/public/favicon.png` para la portada.

Capturas opcionales si el docente solicita evidencia adicional:

- `captura_tests_maven.png`: terminal o reporte donde se vea `mvnw test` exitoso.
- `captura_jacoco_cobertura.png`: reporte JaCoCo con cobertura de lineas e instrucciones.
- `captura_owasp_dependency_check.png`: reporte OWASP Dependency Check sin vulnerabilidades criticas.
- `captura_jmeter_latencia.png`: resultado JMeter o evidencia del promedio de 185 ms.
